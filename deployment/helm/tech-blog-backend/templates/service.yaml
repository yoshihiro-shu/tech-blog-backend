apiVersion: v1
kind: Service
metadata:
  name: {{ include "tech-blog-backend.fullname" . }}
  labels:
    {{- include "tech-blog-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tech-blog-backend.selectorLabels" . | nindent 4 }}
